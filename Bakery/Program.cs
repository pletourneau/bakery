using System;
using System.Collections.Generic;
using Bakery.Models;

namespace Bakery
{
  class Program
  {
    static void Main()
    {
      Console.WriteLine("*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*");
      Console.WriteLine("Welcome to your Bakery!");
      Console.WriteLine("Each bread loaf is $5, and every third loaf is free");
      Console.WriteLine("Each pastry is $5, and every fourth loaf is free");
      Console.WriteLine("How many pastries and loaves of bread would you like?");
    }
  }
}